package simulation

import org.scalatest.FunSuite
import simulation.agents.{Agent, VelocityCalc}
import simulation.utils.Vector2D

import scala.collection.mutable.ArrayBuffer

class VelocityCalcTest extends FunSuite {

  test("testRotate") {

  }

  test("testDesired") {

  }

  test("testAvoidCollision") {

  }

  test("testInPersonalSpace") {

  }

  test("testVelocity") {

  }

  test("testIsFontCollisionDanger") {
    /*
    var agents: ArrayBuffer[Agent] = ArrayBuffer()
    val neighbour = new Agent(Vector2D(1,2), Vector2D(1,50), 20, agents)
    agents += neighbour
    val agent = new Agent(Vector2D(1,1), Vector2D(1,50), 20, agents)

    val calc = VelocityCalc(agent)
    val res1 = calc.isFontCollisionDanger(Vector2D(0, 10))

    assert(res1)
    */
  }
}
